express generator(generate project)

```express --no-view --git server```

prisma generator(generate db)

```npx prisma init --datasource-provider sqlite```

prisma migration

```npx prisma migrate dev --name init```

